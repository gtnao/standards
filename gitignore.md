# .gitignoreの初期設定

Node.js・TypeScriptの現在の構成では、次をプロジェクトルートの`.gitignore`に置く。

```gitignore
node_modules/
dist/

.env
.env.*
!.env.example
```

## 各項目の意味と採用理由

| 項目 | 意味・理由 |
| --- | --- |
| `node_modules/` | インストールした依存パッケージを除外する |
| `dist/` | [tsconfigで指定したビルド出力](tsconfig.md)を除外する |
| `.env`・`.env.*` | ローカルの設定値や秘密情報を除外する。`.env.local`なども対象になる |
| `!.env.example` | 共有する雛形を除外対象から戻す |

`!`は、それまでの除外を取り消す指定。例外は除外指定より後に書く。
この例は先頭に`/`を付けていないため、サブディレクトリ内の同名ファイル・ディレクトリにも適用される。
ただし、親ディレクトリ自体が除外されている場合、その中のファイルだけを`!`で戻すことはできない。

`.env.example`には実際の秘密情報を入れない。内容や読み込み方は[環境変数の扱い](env.md)を参照。

## 除外を追加する方針

採用したツールや生成物に合わせて追加する。例えば、カバレッジを出すようになったら`coverage/`、Next.jsを導入したら`.next/`を追加する。
使っていないツールの除外を最初から並べる必要はない。

OSやエディターなど、個人環境由来のファイルは原則として個人のグローバルignoreで扱う。
チームで共有するエディター設定まで一括除外しない。

すべてを除外して必要なものだけ戻す方式は採用しない。
新しいソースや設定の追加漏れがGit上で見えにくくなるため。Dockerへの転送対象を絞る`.dockerignore`とは目的を分けて判断する。

[BiomeのGit連携](biome.md)でもこの除外を利用するため、生成物などの除外を二重管理しない。

## 既に追跡しているファイル

`.gitignore`は、既にGitで追跡しているファイルには効かない。
後から除外する場合は、`.gitignore`への追加に加えて追跡対象から外す必要がある。
追跡を外しても過去のコミットからは消えないため、秘密情報をコミットした場合は、その情報の失効・再発行も必要になる。

## 参考

- [Git：gitignoreの仕様と除外設定の使い分け](https://git-scm.com/docs/gitignore)
